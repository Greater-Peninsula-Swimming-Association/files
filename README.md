# files

Static file hosting for GPSA documents, served via GitHub Pages at `files.gpsaswimming.org`.

Drop files into a subdirectory and commit. The deploy workflow auto-generates browsable `index.html` pages for each directory.

## Structure

```
forms/    — Printable forms for registration and meet operations
scripts/  — Index generator (not served)
```
